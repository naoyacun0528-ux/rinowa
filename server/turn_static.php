<?php
/**
 * TURN の中継 — いまは無し。
 *
 * 何が起きたか。ここは以前 `openrelay.metered.ca`（Open Relay Project）を指していた。
 * 資格情報が公開されている無料の公開 TURN で、まさにこういう試験のためのもの。
 *
 * **もう存在しない。** ホスト名が解決しない。塞がれているのでも落ちているのでもなく、
 * 消えている。Metered の現在の窓口（`standard.relay.metered.ca`、
 * `global.relay.metered.ca`）は生きているが、アカウントとアプリごとの資格情報が要る。
 * つまり、資格情報なしで頼れる公開の中継はもう無い。
 *
 * 名前を付けておくべき失敗。死んだホストを、解決するかどうか一度も確かめずに設定した。
 * そのせいで、他人のせいに見える失敗が出た。どこからも届かない中継を塞いだとして、
 * ホテルの回線が疑われた。
 *
 * これは Rinowa Direct の `ACCESS_LOCAL_NETWORK` と同じ形の間違い。どこかに書いてあるから
 * その能力があると仮定して、尋ねなかった。**規則も同じ。確かめてから頼る。**
 *
 * 帰結は隠さずに書く。中継が無いので ice.php は STUN だけを返す。アプリはそれを検出し
 * （`IceServers.hasRelay`）、誰かがかける前に通話画面でそう言う。黙って失敗させない。
 * 両端が普通の NAT なら通話はできる。どちらかが対称 NAT だと失敗する — ホテル、学校、
 * オフィス、そしてモバイル回線のほとんど。
 *
 * 置き換え方。`use-auth-secret` の coturn なら `turn_secret.php` に
 * `['host' => ..., 'secret' => ...]` を返させる（ice.php はそちらを優先し、短命の
 * HMAC 資格情報を発行する）。あるいは提供元の固定の資格情報を下の配列に入れる。
 *
 * **先にホスト名が解決することを確かめること。**
 */

declare(strict_types=1);

return [];
